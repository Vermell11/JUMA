# dependencies
node_modules/
# build output
dist/
# astro
.astro/
# environment
.env
.env.production
.env.*.local
# macOS
.DS_Store
# logs
npm-debug.log*
yarn-error.log*
# Python cache
__pycache__/
*.pyc
# Editor / tooling config (personal — not part of the project)
.claude/
.vscode/
.idea/

# Raw video sources — backups only, too heavy for git.
# The site uses the generated frames in frontend/public/hero/ instead.
frontend/design-source/videos/

# Design delivery package (already integrated into frontend/) — local reference only.
/Public/
